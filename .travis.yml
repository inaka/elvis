sudo: false
language: erlang
otp_release:
  - 19.2
  - 20.3
  - 21.3
  - 22.0
before_install:
  - ./ci before_install "${PWD:?}"/rebar3
install:
  - ./ci install "${PWD:?}"/rebar3
script:
  - ./ci script "${PWD:?}"/rebar3
cache:
  directories:
    - .plt
